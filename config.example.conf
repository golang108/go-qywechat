# Created by Yanjunhui

# HTTP service settings.
[http]
address = 0.0.0.0
port = 4567

# Request authentication. Use a long random value and pass it as
# /send?token=<value>, X-Chat-Token, or Authorization: Bearer <value>.
[server]
auth_token = replace-with-a-long-random-token

# WeCom application settings.
[weixin]
CorpID = replace-with-your-corpid
AgentId = 1000001
Secret = replace-with-your-secret
